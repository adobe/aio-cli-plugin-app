{
    "name": "<%= package_name %>",
    "version": "0.0.1",
    "devDependencies": {
        "jest": "^24.8.0"
    },
    "scripts": {
        "test": "jest --ci -w=2"
    },
    "dependencies": {
        "jest-junit": "^6.4.0"
    }
}